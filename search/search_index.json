{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"ofdtd","text":"<p>Welcome to the documentation for <code>ofdtd</code>.</p>"},{"location":"#installation","title":"Installation","text":"<pre><code>pip install ofdtd\n</code></pre>"},{"location":"#development-install","title":"Development install","text":"<pre><code>pip install -e \".[dev,docs]\"\n</code></pre>"},{"location":"#api-reference","title":"API reference","text":"<p>The API reference is generated automatically from public Python functions, classes, and docstrings in <code>src/ofdtd/</code>.</p>"},{"location":"reference/SUMMARY/","title":"SUMMARY","text":"<ul> <li>ofdtd<ul> <li>base</li> </ul> </li> </ul>"},{"location":"reference/ofdtd/","title":"<code>ofdtd</code>","text":"<p>Octree-based finite-difference time-domain tools.</p>"},{"location":"reference/ofdtd/#ofdtd.OFDTDBaseModel","title":"OFDTDBaseModel","text":"<p>               Bases: <code>BaseModel</code></p> <p>Immutable, recursively serializable base class for OFDTD objects.</p> <p>Subclasses may contain other <code>OFDTDBaseModel</code> instances and may contain <code>xarray.DataArray</code> or <code>xarray.Dataset</code> instances anywhere in the model tree, including inside lists, tuples, or dictionaries.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; import xarray as xr\n&gt;&gt;&gt; class FieldData(OFDTDBaseModel):\n...     ex: xr.DataArray\n&gt;&gt;&gt; fd = FieldData(ex=xr.DataArray([1.0, 2.0], dims=(\"x\",)))\n&gt;&gt;&gt; fd.write_hdf5(\"field.h5\", overwrite=True)\n&gt;&gt;&gt; round_trip = FieldData.read_hdf5(\"field.h5\")\n&gt;&gt;&gt; round_trip.ex.equals(fd.ex)\nTrue\n</code></pre> Source code in <code>src/ofdtd/base.py</code> <pre><code>class OFDTDBaseModel(BaseModel):\n    \"\"\"Immutable, recursively serializable base class for OFDTD objects.\n\n    Subclasses may contain other ``OFDTDBaseModel`` instances and may contain\n    ``xarray.DataArray`` or ``xarray.Dataset`` instances anywhere in the model\n    tree, including inside lists, tuples, or dictionaries.\n\n    Examples\n    --------\n    &gt;&gt;&gt; import xarray as xr\n    &gt;&gt;&gt; class FieldData(OFDTDBaseModel):\n    ...     ex: xr.DataArray\n    &gt;&gt;&gt; fd = FieldData(ex=xr.DataArray([1.0, 2.0], dims=(\"x\",)))\n    &gt;&gt;&gt; fd.write_hdf5(\"field.h5\", overwrite=True)\n    &gt;&gt;&gt; round_trip = FieldData.read_hdf5(\"field.h5\")\n    &gt;&gt;&gt; round_trip.ex.equals(fd.ex)\n    True\n    \"\"\"\n\n    model_config = ConfigDict(\n        arbitrary_types_allowed=True,\n        extra=\"forbid\",\n        frozen=True,\n        populate_by_name=True,\n    )\n\n    _registry: ClassVar[dict[str, type[\"OFDTDBaseModel\"]]] = {}\n\n    def __init_subclass__(cls, **kwargs: Any) -&gt; None:\n        \"\"\"Register subclasses for HDF5 deserialization.\n\n        Parameters\n        ----------\n        **kwargs\n            Keyword arguments forwarded to ``pydantic.BaseModel`` subclass\n            initialization.\n        \"\"\"\n\n        super().__init_subclass__(**kwargs)\n        OFDTDBaseModel._registry[_qualified_name(cls)] = cls\n\n    def updated_copy(self: Self, **updates: Any) -&gt; Self:\n        \"\"\"Return a validated copy with selected fields replaced.\n\n        Parameters\n        ----------\n        **updates\n            Field values to replace in the returned model.\n\n        Returns\n        -------\n        Self\n            A new model instance validated by Pydantic.\n\n        Notes\n        -----\n        Pydantic's ``model_copy(update=...)`` intentionally skips validation of\n        updated values. This helper reconstructs the model through\n        ``model_validate`` instead.\n        \"\"\"\n\n        data = {\n            field_name: getattr(self, field_name)\n            for field_name in type(self).model_fields\n        }\n        data.update(updates)\n        return type(self).model_validate(data)\n\n    def write_hdf5(\n        self,\n        path: str | Path,\n        *,\n        overwrite: bool = False,\n        compression: str | None = \"gzip\",\n        compression_opts: int | None = 4,\n    ) -&gt; None:\n        \"\"\"Write this model tree to an HDF5 file.\n\n        Parameters\n        ----------\n        path\n            Destination ``.h5`` or ``.hdf5`` path.\n        overwrite\n            If ``False``, raise ``FileExistsError`` when ``path`` already\n            exists.\n        compression\n            HDF5 compression filter applied to non-scalar numeric array data.\n            Set to ``None`` to disable compression.\n        compression_opts\n            Compression level or options forwarded to h5py.\n\n        Raises\n        ------\n        FileExistsError\n            If ``path`` exists and ``overwrite`` is ``False``.\n        TypeError\n            If a field contains an unsupported value, including a raw\n            ``numpy.ndarray`` outside an xarray object.\n        \"\"\"\n\n        path = Path(path)\n        if path.exists() and not overwrite:\n            raise FileExistsError(f\"Refusing to overwrite existing file: {path}\")\n        path.parent.mkdir(parents=True, exist_ok=True)\n\n        with h5py.File(path, \"w\") as h5:\n            writer = _HDF5ModelWriter(\n                h5=h5,\n                compression=compression,\n                compression_opts=compression_opts,\n            )\n            metadata = _encode_value(self, writer)\n            writer.write_metadata(metadata)\n\n    @classmethod\n    def read_hdf5(cls: type[Self], path: str | Path) -&gt; Self:\n        \"\"\"Read an OFDTD model tree from an HDF5 file.\n\n        Parameters\n        ----------\n        path\n            HDF5 file path to read.\n\n        Returns\n        -------\n        Self\n            The root model reconstructed from the file.\n\n        Raises\n        ------\n        TypeError\n            If the stored root model is not an instance of ``cls``.\n        OFDTDSerializationError\n            If the file does not contain a supported OFDTD model payload.\n        \"\"\"\n\n        path = Path(path)\n        with h5py.File(path, \"r\") as h5:\n            reader = _HDF5ModelReader(h5=h5)\n            metadata = reader.read_metadata()\n            obj = _decode_value(metadata, reader)\n\n        if not isinstance(obj, cls):\n            raise TypeError(\n                f\"HDF5 file contains {_qualified_name(type(obj))}, \"\n                f\"which is not an instance of {_qualified_name(cls)}.\"\n            )\n        return obj\n\n    def to_hdf5(self, path: str | Path, **kwargs: Any) -&gt; None:\n        \"\"\"Write this model tree to an HDF5 file.\n\n        Parameters\n        ----------\n        path\n            Destination ``.h5`` or ``.hdf5`` path.\n        **kwargs\n            Keyword arguments forwarded to :meth:`write_hdf5`.\n        \"\"\"\n\n        self.write_hdf5(path, **kwargs)\n\n    @classmethod\n    def from_hdf5(cls: type[Self], path: str | Path) -&gt; Self:\n        \"\"\"Read an OFDTD model tree from an HDF5 file.\n\n        Parameters\n        ----------\n        path\n            HDF5 file path to read.\n\n        Returns\n        -------\n        Self\n            The root model reconstructed from the file.\n        \"\"\"\n\n        return cls.read_hdf5(path)\n</code></pre>"},{"location":"reference/ofdtd/#ofdtd.OFDTDBaseModel.updated_copy","title":"updated_copy","text":"<pre><code>updated_copy(**updates: Any) -&gt; Self\n</code></pre> <p>Return a validated copy with selected fields replaced.</p> <p>Parameters:</p> Name Type Description Default <code>**updates</code> <code>Any</code> <p>Field values to replace in the returned model.</p> <code>{}</code> <p>Returns:</p> Type Description <code>Self</code> <p>A new model instance validated by Pydantic.</p> Notes <p>Pydantic's <code>model_copy(update=...)</code> intentionally skips validation of updated values. This helper reconstructs the model through <code>model_validate</code> instead.</p> Source code in <code>src/ofdtd/base.py</code> <pre><code>def updated_copy(self: Self, **updates: Any) -&gt; Self:\n    \"\"\"Return a validated copy with selected fields replaced.\n\n    Parameters\n    ----------\n    **updates\n        Field values to replace in the returned model.\n\n    Returns\n    -------\n    Self\n        A new model instance validated by Pydantic.\n\n    Notes\n    -----\n    Pydantic's ``model_copy(update=...)`` intentionally skips validation of\n    updated values. This helper reconstructs the model through\n    ``model_validate`` instead.\n    \"\"\"\n\n    data = {\n        field_name: getattr(self, field_name)\n        for field_name in type(self).model_fields\n    }\n    data.update(updates)\n    return type(self).model_validate(data)\n</code></pre>"},{"location":"reference/ofdtd/#ofdtd.OFDTDBaseModel.write_hdf5","title":"write_hdf5","text":"<pre><code>write_hdf5(path: str | Path, *, overwrite: bool = False, compression: str | None = 'gzip', compression_opts: int | None = 4) -&gt; None\n</code></pre> <p>Write this model tree to an HDF5 file.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str | Path</code> <p>Destination <code>.h5</code> or <code>.hdf5</code> path.</p> required <code>overwrite</code> <code>bool</code> <p>If <code>False</code>, raise <code>FileExistsError</code> when <code>path</code> already exists.</p> <code>False</code> <code>compression</code> <code>str | None</code> <p>HDF5 compression filter applied to non-scalar numeric array data. Set to <code>None</code> to disable compression.</p> <code>'gzip'</code> <code>compression_opts</code> <code>int | None</code> <p>Compression level or options forwarded to h5py.</p> <code>4</code> <p>Raises:</p> Type Description <code>FileExistsError</code> <p>If <code>path</code> exists and <code>overwrite</code> is <code>False</code>.</p> <code>TypeError</code> <p>If a field contains an unsupported value, including a raw <code>numpy.ndarray</code> outside an xarray object.</p> Source code in <code>src/ofdtd/base.py</code> <pre><code>def write_hdf5(\n    self,\n    path: str | Path,\n    *,\n    overwrite: bool = False,\n    compression: str | None = \"gzip\",\n    compression_opts: int | None = 4,\n) -&gt; None:\n    \"\"\"Write this model tree to an HDF5 file.\n\n    Parameters\n    ----------\n    path\n        Destination ``.h5`` or ``.hdf5`` path.\n    overwrite\n        If ``False``, raise ``FileExistsError`` when ``path`` already\n        exists.\n    compression\n        HDF5 compression filter applied to non-scalar numeric array data.\n        Set to ``None`` to disable compression.\n    compression_opts\n        Compression level or options forwarded to h5py.\n\n    Raises\n    ------\n    FileExistsError\n        If ``path`` exists and ``overwrite`` is ``False``.\n    TypeError\n        If a field contains an unsupported value, including a raw\n        ``numpy.ndarray`` outside an xarray object.\n    \"\"\"\n\n    path = Path(path)\n    if path.exists() and not overwrite:\n        raise FileExistsError(f\"Refusing to overwrite existing file: {path}\")\n    path.parent.mkdir(parents=True, exist_ok=True)\n\n    with h5py.File(path, \"w\") as h5:\n        writer = _HDF5ModelWriter(\n            h5=h5,\n            compression=compression,\n            compression_opts=compression_opts,\n        )\n        metadata = _encode_value(self, writer)\n        writer.write_metadata(metadata)\n</code></pre>"},{"location":"reference/ofdtd/#ofdtd.OFDTDBaseModel.read_hdf5","title":"read_hdf5  <code>classmethod</code>","text":"<pre><code>read_hdf5(path: str | Path) -&gt; Self\n</code></pre> <p>Read an OFDTD model tree from an HDF5 file.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str | Path</code> <p>HDF5 file path to read.</p> required <p>Returns:</p> Type Description <code>Self</code> <p>The root model reconstructed from the file.</p> <p>Raises:</p> Type Description <code>TypeError</code> <p>If the stored root model is not an instance of <code>cls</code>.</p> <code>OFDTDSerializationError</code> <p>If the file does not contain a supported OFDTD model payload.</p> Source code in <code>src/ofdtd/base.py</code> <pre><code>@classmethod\ndef read_hdf5(cls: type[Self], path: str | Path) -&gt; Self:\n    \"\"\"Read an OFDTD model tree from an HDF5 file.\n\n    Parameters\n    ----------\n    path\n        HDF5 file path to read.\n\n    Returns\n    -------\n    Self\n        The root model reconstructed from the file.\n\n    Raises\n    ------\n    TypeError\n        If the stored root model is not an instance of ``cls``.\n    OFDTDSerializationError\n        If the file does not contain a supported OFDTD model payload.\n    \"\"\"\n\n    path = Path(path)\n    with h5py.File(path, \"r\") as h5:\n        reader = _HDF5ModelReader(h5=h5)\n        metadata = reader.read_metadata()\n        obj = _decode_value(metadata, reader)\n\n    if not isinstance(obj, cls):\n        raise TypeError(\n            f\"HDF5 file contains {_qualified_name(type(obj))}, \"\n            f\"which is not an instance of {_qualified_name(cls)}.\"\n        )\n    return obj\n</code></pre>"},{"location":"reference/ofdtd/#ofdtd.OFDTDBaseModel.to_hdf5","title":"to_hdf5","text":"<pre><code>to_hdf5(path: str | Path, **kwargs: Any) -&gt; None\n</code></pre> <p>Write this model tree to an HDF5 file.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str | Path</code> <p>Destination <code>.h5</code> or <code>.hdf5</code> path.</p> required <code>**kwargs</code> <code>Any</code> <p>Keyword arguments forwarded to :meth:<code>write_hdf5</code>.</p> <code>{}</code> Source code in <code>src/ofdtd/base.py</code> <pre><code>def to_hdf5(self, path: str | Path, **kwargs: Any) -&gt; None:\n    \"\"\"Write this model tree to an HDF5 file.\n\n    Parameters\n    ----------\n    path\n        Destination ``.h5`` or ``.hdf5`` path.\n    **kwargs\n        Keyword arguments forwarded to :meth:`write_hdf5`.\n    \"\"\"\n\n    self.write_hdf5(path, **kwargs)\n</code></pre>"},{"location":"reference/ofdtd/#ofdtd.OFDTDBaseModel.from_hdf5","title":"from_hdf5  <code>classmethod</code>","text":"<pre><code>from_hdf5(path: str | Path) -&gt; Self\n</code></pre> <p>Read an OFDTD model tree from an HDF5 file.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str | Path</code> <p>HDF5 file path to read.</p> required <p>Returns:</p> Type Description <code>Self</code> <p>The root model reconstructed from the file.</p> Source code in <code>src/ofdtd/base.py</code> <pre><code>@classmethod\ndef from_hdf5(cls: type[Self], path: str | Path) -&gt; Self:\n    \"\"\"Read an OFDTD model tree from an HDF5 file.\n\n    Parameters\n    ----------\n    path\n        HDF5 file path to read.\n\n    Returns\n    -------\n    Self\n        The root model reconstructed from the file.\n    \"\"\"\n\n    return cls.read_hdf5(path)\n</code></pre>"},{"location":"reference/ofdtd/#ofdtd.OFDTDSerializationError","title":"OFDTDSerializationError","text":"<p>               Bases: <code>RuntimeError</code></p> <p>Raised when an OFDTD object cannot be serialized or deserialized.</p> Source code in <code>src/ofdtd/base.py</code> <pre><code>class OFDTDSerializationError(RuntimeError):\n    \"\"\"Raised when an OFDTD object cannot be serialized or deserialized.\"\"\"\n</code></pre>"},{"location":"reference/ofdtd/base/","title":"<code>ofdtd.base</code>","text":"<p>Base models and HDF5 persistence for OFDTD.</p> <p>The public OFDTD object layer is Pydantic-based, immutable by default, and serializes recursively to a plain HDF5 hierarchy. Numerical arrays are expected to be represented as <code>xarray.DataArray</code> or <code>xarray.Dataset</code> objects so that array dimensions, coordinates, names, and attributes survive round-trip IO.</p> Notes <p>Every <code>OFDTDBaseModel</code> can contain nested <code>OFDTDBaseModel</code> objects. Nested models, xarray objects, collections, and scalar metadata are encoded in a JSON metadata tree under <code>/__ofdtd__/metadata_json</code>. Xarray payloads are stored in <code>/__ofdtd_arrays__</code> and referenced by the metadata tree.</p>"},{"location":"reference/ofdtd/base/#ofdtd.base.OFDTDSerializationError","title":"OFDTDSerializationError","text":"<p>               Bases: <code>RuntimeError</code></p> <p>Raised when an OFDTD object cannot be serialized or deserialized.</p> Source code in <code>src/ofdtd/base.py</code> <pre><code>class OFDTDSerializationError(RuntimeError):\n    \"\"\"Raised when an OFDTD object cannot be serialized or deserialized.\"\"\"\n</code></pre>"},{"location":"reference/ofdtd/base/#ofdtd.base.OFDTDBaseModel","title":"OFDTDBaseModel","text":"<p>               Bases: <code>BaseModel</code></p> <p>Immutable, recursively serializable base class for OFDTD objects.</p> <p>Subclasses may contain other <code>OFDTDBaseModel</code> instances and may contain <code>xarray.DataArray</code> or <code>xarray.Dataset</code> instances anywhere in the model tree, including inside lists, tuples, or dictionaries.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; import xarray as xr\n&gt;&gt;&gt; class FieldData(OFDTDBaseModel):\n...     ex: xr.DataArray\n&gt;&gt;&gt; fd = FieldData(ex=xr.DataArray([1.0, 2.0], dims=(\"x\",)))\n&gt;&gt;&gt; fd.write_hdf5(\"field.h5\", overwrite=True)\n&gt;&gt;&gt; round_trip = FieldData.read_hdf5(\"field.h5\")\n&gt;&gt;&gt; round_trip.ex.equals(fd.ex)\nTrue\n</code></pre> Source code in <code>src/ofdtd/base.py</code> <pre><code>class OFDTDBaseModel(BaseModel):\n    \"\"\"Immutable, recursively serializable base class for OFDTD objects.\n\n    Subclasses may contain other ``OFDTDBaseModel`` instances and may contain\n    ``xarray.DataArray`` or ``xarray.Dataset`` instances anywhere in the model\n    tree, including inside lists, tuples, or dictionaries.\n\n    Examples\n    --------\n    &gt;&gt;&gt; import xarray as xr\n    &gt;&gt;&gt; class FieldData(OFDTDBaseModel):\n    ...     ex: xr.DataArray\n    &gt;&gt;&gt; fd = FieldData(ex=xr.DataArray([1.0, 2.0], dims=(\"x\",)))\n    &gt;&gt;&gt; fd.write_hdf5(\"field.h5\", overwrite=True)\n    &gt;&gt;&gt; round_trip = FieldData.read_hdf5(\"field.h5\")\n    &gt;&gt;&gt; round_trip.ex.equals(fd.ex)\n    True\n    \"\"\"\n\n    model_config = ConfigDict(\n        arbitrary_types_allowed=True,\n        extra=\"forbid\",\n        frozen=True,\n        populate_by_name=True,\n    )\n\n    _registry: ClassVar[dict[str, type[\"OFDTDBaseModel\"]]] = {}\n\n    def __init_subclass__(cls, **kwargs: Any) -&gt; None:\n        \"\"\"Register subclasses for HDF5 deserialization.\n\n        Parameters\n        ----------\n        **kwargs\n            Keyword arguments forwarded to ``pydantic.BaseModel`` subclass\n            initialization.\n        \"\"\"\n\n        super().__init_subclass__(**kwargs)\n        OFDTDBaseModel._registry[_qualified_name(cls)] = cls\n\n    def updated_copy(self: Self, **updates: Any) -&gt; Self:\n        \"\"\"Return a validated copy with selected fields replaced.\n\n        Parameters\n        ----------\n        **updates\n            Field values to replace in the returned model.\n\n        Returns\n        -------\n        Self\n            A new model instance validated by Pydantic.\n\n        Notes\n        -----\n        Pydantic's ``model_copy(update=...)`` intentionally skips validation of\n        updated values. This helper reconstructs the model through\n        ``model_validate`` instead.\n        \"\"\"\n\n        data = {\n            field_name: getattr(self, field_name)\n            for field_name in type(self).model_fields\n        }\n        data.update(updates)\n        return type(self).model_validate(data)\n\n    def write_hdf5(\n        self,\n        path: str | Path,\n        *,\n        overwrite: bool = False,\n        compression: str | None = \"gzip\",\n        compression_opts: int | None = 4,\n    ) -&gt; None:\n        \"\"\"Write this model tree to an HDF5 file.\n\n        Parameters\n        ----------\n        path\n            Destination ``.h5`` or ``.hdf5`` path.\n        overwrite\n            If ``False``, raise ``FileExistsError`` when ``path`` already\n            exists.\n        compression\n            HDF5 compression filter applied to non-scalar numeric array data.\n            Set to ``None`` to disable compression.\n        compression_opts\n            Compression level or options forwarded to h5py.\n\n        Raises\n        ------\n        FileExistsError\n            If ``path`` exists and ``overwrite`` is ``False``.\n        TypeError\n            If a field contains an unsupported value, including a raw\n            ``numpy.ndarray`` outside an xarray object.\n        \"\"\"\n\n        path = Path(path)\n        if path.exists() and not overwrite:\n            raise FileExistsError(f\"Refusing to overwrite existing file: {path}\")\n        path.parent.mkdir(parents=True, exist_ok=True)\n\n        with h5py.File(path, \"w\") as h5:\n            writer = _HDF5ModelWriter(\n                h5=h5,\n                compression=compression,\n                compression_opts=compression_opts,\n            )\n            metadata = _encode_value(self, writer)\n            writer.write_metadata(metadata)\n\n    @classmethod\n    def read_hdf5(cls: type[Self], path: str | Path) -&gt; Self:\n        \"\"\"Read an OFDTD model tree from an HDF5 file.\n\n        Parameters\n        ----------\n        path\n            HDF5 file path to read.\n\n        Returns\n        -------\n        Self\n            The root model reconstructed from the file.\n\n        Raises\n        ------\n        TypeError\n            If the stored root model is not an instance of ``cls``.\n        OFDTDSerializationError\n            If the file does not contain a supported OFDTD model payload.\n        \"\"\"\n\n        path = Path(path)\n        with h5py.File(path, \"r\") as h5:\n            reader = _HDF5ModelReader(h5=h5)\n            metadata = reader.read_metadata()\n            obj = _decode_value(metadata, reader)\n\n        if not isinstance(obj, cls):\n            raise TypeError(\n                f\"HDF5 file contains {_qualified_name(type(obj))}, \"\n                f\"which is not an instance of {_qualified_name(cls)}.\"\n            )\n        return obj\n\n    def to_hdf5(self, path: str | Path, **kwargs: Any) -&gt; None:\n        \"\"\"Write this model tree to an HDF5 file.\n\n        Parameters\n        ----------\n        path\n            Destination ``.h5`` or ``.hdf5`` path.\n        **kwargs\n            Keyword arguments forwarded to :meth:`write_hdf5`.\n        \"\"\"\n\n        self.write_hdf5(path, **kwargs)\n\n    @classmethod\n    def from_hdf5(cls: type[Self], path: str | Path) -&gt; Self:\n        \"\"\"Read an OFDTD model tree from an HDF5 file.\n\n        Parameters\n        ----------\n        path\n            HDF5 file path to read.\n\n        Returns\n        -------\n        Self\n            The root model reconstructed from the file.\n        \"\"\"\n\n        return cls.read_hdf5(path)\n</code></pre>"},{"location":"reference/ofdtd/base/#ofdtd.base.OFDTDBaseModel.updated_copy","title":"updated_copy","text":"<pre><code>updated_copy(**updates: Any) -&gt; Self\n</code></pre> <p>Return a validated copy with selected fields replaced.</p> <p>Parameters:</p> Name Type Description Default <code>**updates</code> <code>Any</code> <p>Field values to replace in the returned model.</p> <code>{}</code> <p>Returns:</p> Type Description <code>Self</code> <p>A new model instance validated by Pydantic.</p> Notes <p>Pydantic's <code>model_copy(update=...)</code> intentionally skips validation of updated values. This helper reconstructs the model through <code>model_validate</code> instead.</p> Source code in <code>src/ofdtd/base.py</code> <pre><code>def updated_copy(self: Self, **updates: Any) -&gt; Self:\n    \"\"\"Return a validated copy with selected fields replaced.\n\n    Parameters\n    ----------\n    **updates\n        Field values to replace in the returned model.\n\n    Returns\n    -------\n    Self\n        A new model instance validated by Pydantic.\n\n    Notes\n    -----\n    Pydantic's ``model_copy(update=...)`` intentionally skips validation of\n    updated values. This helper reconstructs the model through\n    ``model_validate`` instead.\n    \"\"\"\n\n    data = {\n        field_name: getattr(self, field_name)\n        for field_name in type(self).model_fields\n    }\n    data.update(updates)\n    return type(self).model_validate(data)\n</code></pre>"},{"location":"reference/ofdtd/base/#ofdtd.base.OFDTDBaseModel.write_hdf5","title":"write_hdf5","text":"<pre><code>write_hdf5(path: str | Path, *, overwrite: bool = False, compression: str | None = 'gzip', compression_opts: int | None = 4) -&gt; None\n</code></pre> <p>Write this model tree to an HDF5 file.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str | Path</code> <p>Destination <code>.h5</code> or <code>.hdf5</code> path.</p> required <code>overwrite</code> <code>bool</code> <p>If <code>False</code>, raise <code>FileExistsError</code> when <code>path</code> already exists.</p> <code>False</code> <code>compression</code> <code>str | None</code> <p>HDF5 compression filter applied to non-scalar numeric array data. Set to <code>None</code> to disable compression.</p> <code>'gzip'</code> <code>compression_opts</code> <code>int | None</code> <p>Compression level or options forwarded to h5py.</p> <code>4</code> <p>Raises:</p> Type Description <code>FileExistsError</code> <p>If <code>path</code> exists and <code>overwrite</code> is <code>False</code>.</p> <code>TypeError</code> <p>If a field contains an unsupported value, including a raw <code>numpy.ndarray</code> outside an xarray object.</p> Source code in <code>src/ofdtd/base.py</code> <pre><code>def write_hdf5(\n    self,\n    path: str | Path,\n    *,\n    overwrite: bool = False,\n    compression: str | None = \"gzip\",\n    compression_opts: int | None = 4,\n) -&gt; None:\n    \"\"\"Write this model tree to an HDF5 file.\n\n    Parameters\n    ----------\n    path\n        Destination ``.h5`` or ``.hdf5`` path.\n    overwrite\n        If ``False``, raise ``FileExistsError`` when ``path`` already\n        exists.\n    compression\n        HDF5 compression filter applied to non-scalar numeric array data.\n        Set to ``None`` to disable compression.\n    compression_opts\n        Compression level or options forwarded to h5py.\n\n    Raises\n    ------\n    FileExistsError\n        If ``path`` exists and ``overwrite`` is ``False``.\n    TypeError\n        If a field contains an unsupported value, including a raw\n        ``numpy.ndarray`` outside an xarray object.\n    \"\"\"\n\n    path = Path(path)\n    if path.exists() and not overwrite:\n        raise FileExistsError(f\"Refusing to overwrite existing file: {path}\")\n    path.parent.mkdir(parents=True, exist_ok=True)\n\n    with h5py.File(path, \"w\") as h5:\n        writer = _HDF5ModelWriter(\n            h5=h5,\n            compression=compression,\n            compression_opts=compression_opts,\n        )\n        metadata = _encode_value(self, writer)\n        writer.write_metadata(metadata)\n</code></pre>"},{"location":"reference/ofdtd/base/#ofdtd.base.OFDTDBaseModel.read_hdf5","title":"read_hdf5  <code>classmethod</code>","text":"<pre><code>read_hdf5(path: str | Path) -&gt; Self\n</code></pre> <p>Read an OFDTD model tree from an HDF5 file.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str | Path</code> <p>HDF5 file path to read.</p> required <p>Returns:</p> Type Description <code>Self</code> <p>The root model reconstructed from the file.</p> <p>Raises:</p> Type Description <code>TypeError</code> <p>If the stored root model is not an instance of <code>cls</code>.</p> <code>OFDTDSerializationError</code> <p>If the file does not contain a supported OFDTD model payload.</p> Source code in <code>src/ofdtd/base.py</code> <pre><code>@classmethod\ndef read_hdf5(cls: type[Self], path: str | Path) -&gt; Self:\n    \"\"\"Read an OFDTD model tree from an HDF5 file.\n\n    Parameters\n    ----------\n    path\n        HDF5 file path to read.\n\n    Returns\n    -------\n    Self\n        The root model reconstructed from the file.\n\n    Raises\n    ------\n    TypeError\n        If the stored root model is not an instance of ``cls``.\n    OFDTDSerializationError\n        If the file does not contain a supported OFDTD model payload.\n    \"\"\"\n\n    path = Path(path)\n    with h5py.File(path, \"r\") as h5:\n        reader = _HDF5ModelReader(h5=h5)\n        metadata = reader.read_metadata()\n        obj = _decode_value(metadata, reader)\n\n    if not isinstance(obj, cls):\n        raise TypeError(\n            f\"HDF5 file contains {_qualified_name(type(obj))}, \"\n            f\"which is not an instance of {_qualified_name(cls)}.\"\n        )\n    return obj\n</code></pre>"},{"location":"reference/ofdtd/base/#ofdtd.base.OFDTDBaseModel.to_hdf5","title":"to_hdf5","text":"<pre><code>to_hdf5(path: str | Path, **kwargs: Any) -&gt; None\n</code></pre> <p>Write this model tree to an HDF5 file.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str | Path</code> <p>Destination <code>.h5</code> or <code>.hdf5</code> path.</p> required <code>**kwargs</code> <code>Any</code> <p>Keyword arguments forwarded to :meth:<code>write_hdf5</code>.</p> <code>{}</code> Source code in <code>src/ofdtd/base.py</code> <pre><code>def to_hdf5(self, path: str | Path, **kwargs: Any) -&gt; None:\n    \"\"\"Write this model tree to an HDF5 file.\n\n    Parameters\n    ----------\n    path\n        Destination ``.h5`` or ``.hdf5`` path.\n    **kwargs\n        Keyword arguments forwarded to :meth:`write_hdf5`.\n    \"\"\"\n\n    self.write_hdf5(path, **kwargs)\n</code></pre>"},{"location":"reference/ofdtd/base/#ofdtd.base.OFDTDBaseModel.from_hdf5","title":"from_hdf5  <code>classmethod</code>","text":"<pre><code>from_hdf5(path: str | Path) -&gt; Self\n</code></pre> <p>Read an OFDTD model tree from an HDF5 file.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str | Path</code> <p>HDF5 file path to read.</p> required <p>Returns:</p> Type Description <code>Self</code> <p>The root model reconstructed from the file.</p> Source code in <code>src/ofdtd/base.py</code> <pre><code>@classmethod\ndef from_hdf5(cls: type[Self], path: str | Path) -&gt; Self:\n    \"\"\"Read an OFDTD model tree from an HDF5 file.\n\n    Parameters\n    ----------\n    path\n        HDF5 file path to read.\n\n    Returns\n    -------\n    Self\n        The root model reconstructed from the file.\n    \"\"\"\n\n    return cls.read_hdf5(path)\n</code></pre>"}]}